import React from "react";
import Layout from "../components/Layout";
import '../css/Home.css';

const Home = (props) => {
  return (
      <Layout sidebar>
      </Layout>
  );
};

export default Home;
